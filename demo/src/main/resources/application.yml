server:
  port: 11000
spring:
  aop:
    auto: true #启动aop配置
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    password:
    jedis:
      timeout: 0
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

